{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://pursekeeper.dev/examples/schemas/work-output.json",
  "title": "POST /v1/work response (200)",
  "type": "object",
  "required": ["work", "difficulty"],
  "properties": {
    "work": {"type": "string", "pattern": "^[0-9a-f]{16}$", "description": "The 8-byte work value, hex, valid for the submitted hash at the returned difficulty."},
    "difficulty": {"type": "string", "pattern": "^[0-9a-f]{16}$", "description": "The difficulty the returned work reaches, hex."},
    "multiplier": {"type": ["number", "string"], "description": "Difficulty relative to the base threshold."},
    "ms": {"type": "integer", "description": "Server compute time in milliseconds."},
    "source": {"type": "string", "description": "Which worker computed it (gpu, node, precomputed)."}
  }
}
